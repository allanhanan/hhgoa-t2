services:
  rag-pipeline:
    container_name: rag-pipeline
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
      - MODELS_DIR=/app/models
    deploy:
      resources:
        limits:
          memory: 8G
