services:
  rag-pipeline:
    build: .
    ports:
      - "8000:8000"
      - "8081:8081"
    volumes:
      - ./data:/app/data
    env_file: .env
    deploy:
      resources:
        limits:
          memory: 8G
